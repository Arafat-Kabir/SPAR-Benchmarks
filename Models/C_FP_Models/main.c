#include "mlp1.h"
#include "lstm128.h"
#include "fplib.h"
#include <stdio.h>

int main() {
    // void *p = 0;
    // initialize(p);
    run_inference();
    // something else: softmax
    // run_inference_lstm128();
    // fpvar a = {-20, 3, 2};
    // fpvar b = {-20, 3, 2};
    // fpvar c = fp_add(a, b);
    // printf("%llX\n", c);
    // printf("%lld\n", c);
    // double t[] = {17.131854, 0.1};
    // fixp tti[2];
    // float_to_fixed(&t, 2, &tti);
    // printf("\n %lf, %lf; \t %d,%d\n", t[0], t[1], tti[0], tti[1]);
    return 0;
}
